<%= render 'cards/form', card: card do |form| %>
	<%= form.hidden_field :actable_type %>
      <%= form.fields_for :actable, card.actable do |a| %>
         <div class="field">
            <%= a.label :completed %>
            <%= a.check_box :completed %>
         </div>
         <div class="field">
            <%= a.label :quest %>
            <%= a.collection_select :quest_id, CardType::Quest.all, :id, :title %>
         </div>
      <% end %>
<% end %>

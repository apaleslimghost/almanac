<div>
	<% quests.each do |quest| %>
		<div>
			<%= ornamented :dotted_rune, as: 'h3' do %>
				<%= quest.title %>
			<% end %>

			<%= render 'objectives/list', objectives: quest.objectives %>
		</div>
	<% end %>
</div>

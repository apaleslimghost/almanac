<% level ||= 1 %>
<% title ||= nil %>

<h<%= level %>>
   <% if icon %>
      <i class="<%= icon %>" aria-role="image" aria-label="<%= label %>"></i>
   <% end %>

   <%= title || yield %>
</h<%= level %>>

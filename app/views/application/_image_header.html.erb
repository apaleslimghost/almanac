<% image_class ||= '' %>
<% inline_if_no_image ||= false %>

<% if image %>
	<%= render image.specific, { tag_name: 'header' }.reverse_merge(local_assigns) do %>
		<div class="splash-wrapper">
			<div class="splash-content">
				<%= yield %>
			</div>
		</div>
	<% end %>
<% else %>
	<header class="<%= image_class %>">
		<% unless inline_if_no_image %>
			<div class="splash-wrapper">
				<div class="splash-content">
		<% end %>
				<%= yield %>
		<% unless inline_if_no_image %>
				</div>
			</div>
		<% end %>
	</header>
<% end %>

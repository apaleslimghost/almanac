<% image_class ||= '' %>

<% if image %>
	<%= render image.specific, { tag_name: 'header' }.reverse_merge(local_assigns) do %>
		<%= yield %>
	<% end %>
<% else %>
	<header class="<%= image_class %>"><%= yield %></header>
<% end %>

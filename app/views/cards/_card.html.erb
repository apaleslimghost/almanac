<%= link_to [card.campaign, card.specific], class: "card" do %>
	<%= render 'image_header', image: card.image, inline_if_no_image: true do %>
		<h3><%= card.title %></h3>
	<% end %>

	<%= card.html_body %>
	<%= yield %>

	<% if local_assigns.key? :link %>
		<%= link.link_type.capitalize %>
		<%= button_to 'Unlink', [card.campaign, card, link], method: :delete %>
	<% end %>
<% end %>
